import React from 'react';
import { View, Text } from 'react-native'

const Farms = props => {
  return (
    <View style={{ flex: 1, justifyContent: 'center', alignItems: 'center' }}>
      <Text>Your Farms</Text>
    </View>
  );
};

export default Farms;